version: "3"

services:
  nfe-ba-service:
    image: guilhermejr/nfe-ba-service
    container_name: nfe-ba-service
    restart: always
    ports:
      - "8000:80"

networks:
  default:
    external:
      name: rede
